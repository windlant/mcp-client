# Model settings
model:
  provider: "deepseek"
  model_name: "deepseek-chat"
  temperature: 0.7
  max_tokens: 1024

# Context management
context:
  max_history: 10  # 最多保留 10 轮对话（用户+AI 为 1 轮）

# Tool settings (预留)
tools:
  enabled: true

# API Key will be loaded from config.local.yaml
# api_key: ""