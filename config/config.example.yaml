model:
  provider: "deepseek"
  api_key: "your_deepseek_api_key_here"
  model_name: "deepseek-chat"
  temperature: 0.7
  max_tokens: 1024

context:
  max_history: 20

tools:
  enabled: true
  mode: "local" # 使用 "local" 或 "stdio"；"remote" 尚未实现